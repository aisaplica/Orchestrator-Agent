---
description: "Genera un dashboard HTML con estadísticas del pipeline ScacsWeb (ejecuciones, éxito, agentes)."
argument-hint: ""
---

Invoke the `orchestrator-skill-full:orchestrator-agent` skill.

After loading the skill and resolving SKILL_DIR (PASO 0), read the agent file inline:
`Read $SKILL_DIR\agents\dashboard.md`

Follow those instructions exactly.

Usage: `/orchestrator-dashboard`
Example: `/orchestrator-dashboard`
