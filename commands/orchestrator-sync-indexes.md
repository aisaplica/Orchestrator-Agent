---
description: "Sincroniza índices Oracle al modelo BD JSON del workspace."
argument-hint: "[workspace_path]"
---

Invoke the `orchestrator-skill-full:orchestrator-agent` skill.

After loading the skill and resolving SKILL_DIR (PASO 0), read the agent file inline:
`Read $SKILL_DIR\agents\sync-indexes.md`

Follow those instructions exactly.

Usage: `/orchestrator-sync-indexes [workspace_path]`
Example: `/orchestrator-sync-indexes C:\Desarrollo\SVN\ScacsWeb`
